"""Canned SQLFlow responses for mapper tests.

Shapes were captured from live SQLFlow HTTP endpoints: a populated tree
(where parse-time defaults were supplied) and a placeholder tree (where
no defaults were supplied). Trimmed to the smallest form that exercises
the mapper.
"""
